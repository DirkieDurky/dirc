bool arraysMatch(void* a, int aSize, void* b, int bSize)
{
    if (aSize != bSize)
    {
        return false;
    }

    for (int i = 0; i < aSize; i++)
    {
        if (a[i] != b[i])
        {
            return false;
        }
    }

    return true;
}
