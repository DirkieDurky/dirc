int* a = malloc(5);
*a = {4, 5, 2, 8, 10};

for (int i = 0; i < getSize(a); i++) {
    println(intToString(a[i]));
}
printNewline();

a = removeAt(a, getSize(a), 2);

for (int j = 0; j < getSize(a); j++) {
    println(intToString(a[j]));
}
