int* test = malloc(2);

test[0] = 4;
test[1] = 5;

char* string = intToString(getSize(test));
println(string);
free(string);
for (int i = 0; i < getSize(test); i++) {
    string = intToString(test[i]);
    println(string);
    free(string);
}
